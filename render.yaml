services:
  - type: web
    name: lexiscan-backend
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "./start.sh"
    envVars:
      - key: GEMINI_API_KEY
        sync: false
